const config = {
  HOST: process.env.HOST || '127.0.0.1',
  PORT: process.env.PORT || '8080'
}

module.exports = config

// exports const HOST = process.env.HOST || '127.0.0.1'
// exports const PORT = process.env.PORT || '8080'
